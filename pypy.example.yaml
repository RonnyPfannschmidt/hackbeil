


workload: # for testing analysis
  path: file:///home/ronny/.local/var/pypy-clone
  start: 0
  end: 400



include:
- pypy
- trunk

exclude:
- trunk/www
- pypy/trunk/www/

branchmatch:
- ^(pypy/)?trunk
- ^pypy/branch/(?P<branch>[^/]+)
- ^pypy/(?P<branch>dist) # dist was the trunk for some time
- ^pypy/ # the transition

